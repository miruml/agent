#[cfg(test)]
mod tests {
    // external crates
    #[allow(unused_imports)]
    use tracing::{debug, error, info, trace, warn};

pub mod sync {
    use super::*;

pub mod new {
    use super::*;

    #[test]
    fn not_created() {
        assert!(false);
    }
}
}
}