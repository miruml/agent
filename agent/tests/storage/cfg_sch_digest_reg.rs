#[cfg(test)]
mod tests {
    // internal crates
    use config_agent::utils::{hash_json, hash_bytes};
    use serde_json::json;
    // external crates
    #[allow(unused_imports)]
    use tracing::{debug, error, info, trace, warn};

pub mod read_resolved_digest {
    use super::*;

    #[test]
    fn success() {
        assert!(false);
    }
}

}