

// if mqtt is enabled, don't do anything


pub async fn pull_deployments(
    mqtt_enabled: bool,
    http_client: &HTTPClient,
    token: &str,
) -> Result<(), DeployErr> {
    // TODO: implement this
    Ok(())
}

